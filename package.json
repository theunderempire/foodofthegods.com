{
  "private": true,
  "scripts": {
    "prepare": "husky",
    "postinstall": "npm install --prefix api && npm install --prefix web"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.5.3"
  },
  "lint-staged": {
    "**/*.{js,mjs,ts,tsx,json}": "prettier --write"
  }
}
